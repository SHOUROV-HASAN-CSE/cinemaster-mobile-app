import { StyleSheet, Text, View } from "react-native";
import React from "react";

const about = () => {
  return (
    <View>
      <Text className="text-4xl text-center text-accent">about</Text>
    </View>
  );
};

export default about;

const styles = StyleSheet.create({});
